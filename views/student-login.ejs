<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Login - UPHSD Faculty Evaluation</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen">
    <!-- Navigation Bar -->
    <nav class="bg-gradient-to-r from-blue-600 to-indigo-600 text-white shadow-lg">
        <div class="container mx-auto px-4 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-3">
                    <i class="fas fa-graduation-cap text-2xl"></i>
                    <div>
                        <h1 class="text-xl font-bold">UPHSD Faculty Evaluation</h1>
                        <p class="text-xs text-blue-100">Student Evaluation System</p>
                    </div>
                </div>
                <a href="/admin/login" class="bg-white text-blue-600 px-4 py-2 rounded-lg font-semibold hover:bg-blue-50 transition">
                    <i class="fas fa-user-shield mr-2"></i>Admin
                </a>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container mx-auto px-4 py-8">
        <div class="max-w-md mx-auto">
            <!-- Welcome Card -->
            <div class="bg-white rounded-2xl shadow-xl p-8 mb-6">
                <div class="text-center mb-6">
                    <div class="bg-blue-100 w-20 h-20 rounded-full flex items-center justify-center mx-auto mb-4">
                        <i class="fas fa-user-graduate text-blue-600 text-3xl"></i>
                    </div>
                    <h2 class="text-2xl font-bold text-gray-800 mb-2">Welcome, Student!</h2>
                    <p class="text-gray-600">Enter your School ID to view your evaluation subjects</p>
                </div>

                <!-- Flash Messages -->
                <% if (typeof error_msg !== 'undefined' && error_msg.length > 0) { %>
                    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg mb-4">
                        <i class="fas fa-exclamation-circle mr-2"></i><%= error_msg %>
                    </div>
                <% } %>

                <% if (typeof success_msg !== 'undefined' && success_msg.length > 0) { %>
                    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg mb-4">
                        <i class="fas fa-check-circle mr-2"></i><%= success_msg %>
                    </div>
                <% } %>

                <!-- Login Form -->
                <form action="/student/login" method="POST" class="space-y-4">
                    <div>
                        <label for="student_number" class="block text-sm font-semibold text-gray-700 mb-2">
                            <i class="fas fa-id-card mr-2 text-blue-600"></i>School ID
                        </label>
                        <input 
                            type="text" 
                            id="student_number" 
                            name="student_number" 
                            placeholder="00-0000-000" 
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-lg"
                            required
                            pattern="[0-9]{2}-[0-9]{4}-[0-9]{3}"
                            title="Format: 00-0000-000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Format: 00-0000-000</p>
                    </div>

                    <button 
                        type="submit" 
                        class="w-full bg-gradient-to-r from-blue-600 to-indigo-600 text-white py-3 rounded-lg font-semibold hover:from-blue-700 hover:to-indigo-700 transition shadow-lg hover:shadow-xl"
                    >
                        <i class="fas fa-sign-in-alt mr-2"></i>Continue
                    </button>
                </form>
            </div>

            <!-- Info Card -->
            <div class="bg-blue-50 rounded-lg p-6 border border-blue-200">
                <h3 class="font-semibold text-blue-900 mb-3 flex items-center">
                    <i class="fas fa-info-circle mr-2"></i>Instructions
                </h3>
                <ul class="text-sm text-blue-800 space-y-2">
                    <li class="flex items-start">
                        <i class="fas fa-check-circle mr-2 mt-1 text-blue-600"></i>
                        <span>Enter your School ID to access your enrolled subjects</span>
                    </li>
                    <li class="flex items-start">
                        <i class="fas fa-check-circle mr-2 mt-1 text-blue-600"></i>
                        <span>Select a subject to evaluate your professor</span>
                    </li>
                    <li class="flex items-start">
                        <i class="fas fa-check-circle mr-2 mt-1 text-blue-600"></i>
                        <span>Complete all evaluations to help improve our education quality</span>
                    </li>
                    <li class="flex items-start">
                        <i class="fas fa-check-circle mr-2 mt-1 text-blue-600"></i>
                        <span>Your feedback is anonymous and confidential</span>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="text-center py-6 text-gray-600">
        <p class="text-sm">© 2026 UPHSD Molino - College of Computer Studies</p>
    </footer>
</body>
</html>
