<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Evaluation Details - UPHSD Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100">
    <%- include('partials/navbar') %>

    <div class="container mx-auto px-4 py-8 max-w-5xl">
        <div class="mb-6">
            <a href="/admin/evaluations" class="text-blue-600 hover:text-blue-800">
                <i class="fas fa-arrow-left mr-2"></i>Back to Evaluations
            </a>
        </div>

        <div class="bg-white rounded-lg shadow-md p-8">
            <h1 class="text-2xl font-bold text-gray-800 mb-6">Evaluation Details</h1>

            <!-- Student Info -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8 pb-6 border-b">
                <div>
                    <p class="text-sm text-gray-500">Student Number</p>
                    <p class="font-semibold"><%= evaluation.student_number %></p>
                </div>
                <div>
                    <p class="text-sm text-gray-500">School Year</p>
                    <p class="font-semibold"><%= evaluation.school_year %></p>
                </div>
                <div>
                    <p class="text-sm text-gray-500">Program</p>
                    <p class="font-semibold"><%= evaluation.program_name %></p>
                </div>
                <div>
                    <p class="text-sm text-gray-500">Year Level</p>
                    <p class="font-semibold"><%= evaluation.year_level %></p>
                </div>
                <div>
                    <p class="text-sm text-gray-500">Status</p>
                    <p class="font-semibold"><%= evaluation.status %></p>
                </div>
                <div>
                    <p class="text-sm text-gray-500">Submitted</p>
                    <p class="font-semibold"><%= new Date(evaluation.submitted_at).toLocaleString() %></p>
                </div>
            </div>

            <!-- Teacher & Course Info -->
            <div class="mb-8 pb-6 border-b">
                <h2 class="text-xl font-bold text-gray-800 mb-4">Teacher & Course Information</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <p class="text-sm text-gray-500">Teacher</p>
                        <p class="font-semibold"><%= evaluation.teacher_name %></p>
                        <% if (evaluation.employee_id) { %>
                            <p class="text-sm text-gray-600">ID: <%= evaluation.employee_id %></p>
                        <% } %>
                    </div>
                    <div>
                        <p class="text-sm text-gray-500">Course</p>
                        <p class="font-semibold"><%= evaluation.course_name %></p>
                    </div>
                </div>
            </div>

            <!-- Ratings -->
            <div class="space-y-8">
                <!-- The Teacher -->
                <div>
                    <h2 class="text-xl font-bold text-gray-800 mb-4 border-l-4 border-blue-500 pl-3">The Teacher</h2>
                    <div class="space-y-3">
                        <div class="flex justify-between items-center bg-gray-50 p-3 rounded">
                            <span class="text-sm">Clarity of diction</span>
                            <span class="font-bold text-lg <%= evaluation.teacher_diction >= 4 ? 'text-green-600' : evaluation.teacher_diction >= 3 ? 'text-yellow-600' : 'text-red-600' %>">
                                <%= evaluation.teacher_diction %>/5
                            </span>
                        </div>
                        <div class="flex justify-between items-center bg-gray-50 p-3 rounded">
                            <span class="text-sm">Grammar usage</span>
                            <span class="font-bold text-lg <%= evaluation.teacher_grammar >= 4 ? 'text-green-600' : evaluation.teacher_grammar >= 3 ? 'text-yellow-600' : 'text-red-600' %>">
                                <%= evaluation.teacher_grammar %>/5
                            </span>
                        </div>
                        <div class="flex justify-between items-center bg-gray-50 p-3 rounded">
                            <span class="text-sm">Personality</span>
                            <span class="font-bold text-lg <%= evaluation.teacher_personality >= 4 ? 'text-green-600' : evaluation.teacher_personality >= 3 ? 'text-yellow-600' : 'text-red-600' %>">
                                <%= evaluation.teacher_personality %>/5
                            </span>
                        </div>
                        <div class="flex justify-between items-center bg-gray-50 p-3 rounded">
                            <span class="text-sm">Disposition</span>
                            <span class="font-bold text-lg <%= evaluation.teacher_disposition >= 4 ? 'text-green-600' : evaluation.teacher_disposition >= 3 ? 'text-yellow-600' : 'text-red-600' %>">
                                <%= evaluation.teacher_disposition %>/5
                            </span>
                        </div>
                        <div class="flex justify-between items-center bg-gray-50 p-3 rounded">
                            <span class="text-sm">Dynamic sharing</span>
                            <span class="font-bold text-lg <%= evaluation.teacher_dynamic >= 4 ? 'text-green-600' : evaluation.teacher_dynamic >= 3 ? 'text-yellow-600' : 'text-red-600' %>">
                                <%= evaluation.teacher_dynamic %>/5
                            </span>
                        </div>
                        <div class="flex justify-between items-center bg-gray-50 p-3 rounded">
                            <span class="text-sm">Fairness</span>
                            <span class="font-bold text-lg <%= evaluation.teacher_fairness >= 4 ? 'text-green-600' : evaluation.teacher_fairness >= 3 ? 'text-yellow-600' : 'text-red-600' %>">
                                <%= evaluation.teacher_fairness %>/5
                            </span>
                        </div>
                    </div>
                </div>

                <!-- Learning Process (abbreviated for brevity) -->
                <div>
                    <h2 class="text-xl font-bold text-gray-800 mb-4 border-l-4 border-green-500 pl-3">Learning Process</h2>
                    <div class="grid grid-cols-2 md:grid-cols-3 gap-3">
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Motivation</p>
                            <p class="font-bold text-lg"><%= evaluation.learning_motivation %>/5</p>
                        </div>
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Critical Thinking</p>
                            <p class="font-bold text-lg"><%= evaluation.learning_critical_thinking %>/5</p>
                        </div>
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Organization</p>
                            <p class="font-bold text-lg"><%= evaluation.learning_organization %>/5</p>
                        </div>
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Explanation</p>
                            <p class="font-bold text-lg"><%= evaluation.learning_explanation %>/5</p>
                        </div>
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Mastery</p>
                            <p class="font-bold text-lg"><%= evaluation.learning_mastery %>/5</p>
                        </div>
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Methodology</p>
                            <p class="font-bold text-lg"><%= evaluation.learning_methodology %>/5</p>
                        </div>
                    </div>
                </div>

                <!-- Classroom Management -->
                <div>
                    <h2 class="text-xl font-bold text-gray-800 mb-4 border-l-4 border-purple-500 pl-3">Classroom Management</h2>
                    <div class="grid grid-cols-2 md:grid-cols-3 gap-3">
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Attendance</p>
                            <p class="font-bold text-lg"><%= evaluation.classroom_attendance %>/5</p>
                        </div>
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Policies</p>
                            <p class="font-bold text-lg"><%= evaluation.classroom_policies %>/5</p>
                        </div>
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Discipline</p>
                            <p class="font-bold text-lg"><%= evaluation.classroom_discipline %>/5</p>
                        </div>
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Authority</p>
                            <p class="font-bold text-lg"><%= evaluation.classroom_authority %>/5</p>
                        </div>
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Prayers</p>
                            <p class="font-bold text-lg"><%= evaluation.classroom_prayers %>/5</p>
                        </div>
                        <div class="bg-gray-50 p-3 rounded text-center">
                            <p class="text-xs text-gray-600 mb-1">Punctuality</p>
                            <p class="font-bold text-lg"><%= evaluation.classroom_punctuality %>/5</p>
                        </div>
                    </div>
                </div>

                <!-- Comments -->
                <% if (evaluation.comments) { %>
                    <div>
                        <h2 class="text-xl font-bold text-gray-800 mb-4">Comments</h2>
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-gray-700"><%= evaluation.comments %></p>
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
