{
  "name": "student-faculty-evaluation",
  "version": "2.0.0",
  "description": "UPHSD Student Faculty Evaluation Form - React + Express",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend",
    "install:all": "npm install && npm run install:backend && npm run install:frontend",

    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run --prefix backend dev",
    "dev:frontend": "npm run --prefix frontend dev",

    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm run --prefix backend build",
    "build:frontend": "npm run --prefix frontend build",

    "start": "npm run --prefix backend start",
    "start:backend": "npm run --prefix backend start",
    "preview:frontend": "npm run --prefix frontend preview",

    "setup-db": "npm run --prefix backend setup-db",

    "heroku-postbuild": "npm run build",
    "test:evaluate": "npx playwright install chromium && ts-node-dev --transpile-only --project tsconfig.json test-student-evaluation.ts",
    "test:evaluate:headless": "npx playwright install chromium && ts-node-dev --transpile-only --project tsconfig.json test-student-evaluation.ts --headless"
  },
  "keywords": [
    "evaluation",
    "faculty",
    "student",
    "react",
    "vite"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^25.2.3",
    "concurrently": "^8.2.2",
    "playwright": "^1.40.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
