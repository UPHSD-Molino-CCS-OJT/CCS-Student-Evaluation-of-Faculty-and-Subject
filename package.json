{
  "name": "student-faculty-evaluation",
  "version": "2.0.0",
  "description": "UPHSD Student Faculty Evaluation Form - React + Express",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "start": "npm run --prefix backend start",
    "dev": "npm run --prefix backend dev",
    "dev:fullstack": "concurrently \"npm run --prefix backend dev\" \"npm run --prefix frontend dev\"",
    "build": "npm run --prefix backend build && npm run --prefix frontend build",
    "setup-db": "npm run --prefix backend setup-db",
    "frontend": "npm run --prefix frontend dev",
    "frontend:build": "npm run --prefix frontend build",
    "heroku-postbuild": "npm run build",
    "test:evaluate": "npx playwright install chromium && ts-node-dev --transpile-only --project tsconfig.json test-student-evaluation.ts",
    "test:evaluate:headless": "npx playwright install chromium && ts-node-dev --transpile-only --project tsconfig.json test-student-evaluation.ts --headless"
  },
  "keywords": [
    "evaluation",
    "faculty",
    "student",
    "react",
    "vite"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^25.2.3",
    "concurrently": "^8.2.2",
    "playwright": "^1.40.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
