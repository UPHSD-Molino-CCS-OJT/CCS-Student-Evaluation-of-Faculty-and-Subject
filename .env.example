# MongoDB Atlas Connection String
# Get this from MongoDB Atlas: Clusters > Connect > Connect your application
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://admin:yourpassword@cluster0.xxxxx.mongodb.net/faculty_evaluation?retryWrites=true&w=majority

# For local MongoDB (if not using Atlas):
# MONGODB_URI=mongodb://localhost:27017/faculty_evaluation

# Session Secret - Change this to a random string for production
SESSION_SECRET=uphsd_faculty_evaluation_secret_key_2026_mongodb

# Server Configuration
PORT=3000

# Environment (development or production)
NODE_ENV=development

# Encryption Key for AES-256-GCM (32 bytes, base64 encoded)
ENCRYPTION_MASTER_KEY=encryption_master_key_2026_mongodb_faculty_evaluation